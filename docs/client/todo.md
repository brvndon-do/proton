# TODO

This will cover the necessary services and components from the agentic client (in this case, it's under `Proton.Agent/`).

As of 08/22/26, this has been put on a pause. The agentic harness will be built and hosted in the engine itself. The client will be repurposed to an observing CLI written in Rust.